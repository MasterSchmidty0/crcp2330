class CellGame {
	field Cell cell;
	field int direction; //0=none,1=up,2=down,3=left,4=right

	constructor CellGame new() {
		let cell = Cell.new(20, 20, 10);
		let direction = 0;
		return this;
	}

	method void run() {
		// TO DO
		return;
	}

	method void dispose() {
		do cell.dispose();
		do Memory.deAlloc(this);
		return;
	}
}